{% extends 'base.html.twig' %}

{% set title = "Check your email"%}
{% set meta_description = "Un lien de confirmation vous a été envoyé par email. Veuillez vérifier votre boîte de réception pour activer votre compte." %}

{% block og_type %}website{% endblock %}
{# --- Directive SEO pour robots --- #}
{% block meta_robots %}noindex, nofollow{% endblock %}

{# --- A ajouter une fois le site terminé --- #}
{# {% block og_image %}/images/og-default.jpg{% endblock %} #} 
{# {% block twitter_image %}/images/twitter-default.jpg{% endblock %} #}
{% block body %}
<main id="content" class="min-h-screen w-full flex flex-col justify-center items-center bg-[url('/img/bg3.jpg')] bg-cover bg-center gap-8">
    <a href="{{ path('app_home') }}">
        <div class="flex flex-row items-center gap-4 text-white">
            <svg width="90" height="73" viewBox="0 0 90 73" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="..." fill="white"/>
            </svg>
            <h1 class="text-5xl font-bold">Collection</h1>
        </div>
    </a>

    <div class="w-3/4 bg-[#CDD5D9] bg-opacity-90 flex flex-col justify-center rounded-lg p-6 text-black-900 text-center">
        <h2 class="text-xl md:text-2xl font-bold mb-4">📬 Password reset email sent</h2>
        <p class="text-base mb-4">
            If an account matching your email exists, a link to reset your password was just sent.
            This link will expire in
            <strong class="text-blue-600">
                {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}
            </strong>.
        </p>
        <p class="text-sm text-gray-700 mb-6">
            If you don’t receive an email, check your spam folder or
            <a href="{{ path('app_forgot_password_request') }}" class="underline text-blue-600 hover:text-blue-800">try again</a>.
        </p>

        <a href="{{ path('app_login') }}" class="block text-white font-bold py-2 px-4 rounded bg-[linear-gradient(to_right,#4AA5F7_0%,#6086FF_100%)] mb-4">
            Return to login
        </a>
    </div>
</main>

<script>
window.addEventListener('turbo:load', () => {
    const element = document.getElementById('content');
    if (element) {
        element.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }
});
</script>
{% endblock %}
