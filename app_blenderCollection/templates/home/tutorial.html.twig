{% extends 'base.html.twig' %}

{% set title = "Tutorial - Blender Collection"%}
{% set meta_description = "Learn how to install and use your Blender add-ons downloaded with Blender Collection through our clear, step-by-step illustrated guide." %}

{% block og_type %}article{% endblock %}
{# --- Directive SEO pour robots --- #}
{% block meta_robots %}index, follow{% endblock %}

{% block body %}
    {# Hero #}
    <section class="relative w-full flex justify-center items-center bg-[url('/img/bg5.webp')] bg-cover bg-center bg-fixed bg-no-repeat">
        <!-- Calque flouté sur toute la section -->
        <div class="absolute inset-0 backdrop-blur-md bg-black/40 z-0"></div>

        <!-- Contenu -->
        <article class="relative z-10 w-full max-w-5xl h-[350px] lg:h-[500px] flex flex-col justify-center px-6 md:px-12">
            <h1 class="font-bold text-4xl md:text-5xl lg:text-7xl text-white leading-tight drop-shadow-lg">
                Blender Collection Tutorial
            </h1>
            <h2 class="text-lg md:text-xl lg:text-2xl mt-3 text-white font-medium drop-shadow">
                Learn how to install, share, and use your add-on packs with ease.
            </h2>
        </article>
    </section>

    {# Expliquer ce qu'est Blender Collection #}
    <section class="p-8 mt-6 flex flex-col gap-6 w-full xl:w-2/3 mb-8 bg-grey-800 rounded-lg">
        <article class="bg-white dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg">
            <h2 class="text-3xl font-semibold dark:text-white text-black-950">What is Blender Collection?</h2>

            <p class="text-lg leading-relaxed dark:text-grey-200 text-white">
                <strong>Blender Collection</strong> is a web application designed to simplify how you manage and share your Blender add-ons.
                It allows you to create personalized collections of extensions, download them all at once, and easily import them into Blender.
            </p>

            <p class="text-lg leading-relaxed dark:text-grey-200 text-white">
                Whether you’re configuring multiple workstations or sharing your setup with the community, Blender Collection ensures
                <strong>portability, consistency, and collaboration</strong>.
            </p>

            <cite class="mt-4 border-l-4 border-purple-500 pl-4 italic text-grey-500">
                Example use case: you build a “Modeling Starter Pack” with your favorite tools — Hard Ops, BoxCutter, and LoopTools.
                With one click, you or your teammates can download and install them all together.
            </cite>
        </article>
    </section>

    {# Comment utiliser Blender Collection #}
    <section class="p-8 mt-8 flex flex-col gap-12 w-full xl:w-2/3 mb-16 bg-grey-800 rounded-lg">
        <h2 class="text-4xl font-semibold text-white">How to Install Your Add-ons</h2>

        {# Étape 1 — Choisir une collection #}
        <article class="bg-white/5 dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg backdrop-blur-sm border border-white/10">
            <h3 class="text-2xl font-semibold mb-2 text-purple-400">1. Browse and Select a Collection</h3>
            <p class="text-lg leading-relaxed text-white">
                Navigate through the available collections on Blender Collection. Each one groups several compatible add-ons.
                Click on the one you want to download.
            </p>

            <figure class="flex flex-col items-center gap-2">
                <img src="/img/tuto/collections-list.png" alt="List of collections" class="rounded-lg shadow-md w-full md:w-4/5 object-cover">
                <figcaption class="text-sm text-grey-400 italic">List of available add-on collections.</figcaption>
            </figure>
        </article>

        {# Étape 2 — Télécharger la collection #}
        <article class="bg-white/5 dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg backdrop-blur-sm border border-white/10">
            <h3 class="text-2xl font-semibold mb-2 text-purple-400">2. Download the Add-on Pack</h3>
            <p class="text-lg leading-relaxed text-white">
                On the collection page, click on the <strong>Download</strong> button to get a <code>.zip</code> file containing all the add-ons.
            </p>

            <div class="grid md:grid-cols-2 gap-6 items-center">
                <figure class="flex flex-col items-center gap-2">
                    <img src="/img/tuto/collection-page.png" alt="Collection page with download button" class="rounded-lg shadow-md object-cover">
                    <figcaption class="text-sm text-grey-400 italic">Click the download button to get your pack.</figcaption>
                </figure>

                <figure class="flex flex-col items-center gap-2">
                    <img src="/img/tuto/download-zip.png" alt="Downloaded zip in browser" class="rounded-lg shadow-md object-cover">
                    <figcaption class="text-sm text-grey-400 italic">The downloaded <code>.zip</code> file.</figcaption>
                </figure>
            </div>
        </article>

        {# Étape 3 — Dézipper les add-ons #}
        <article class="bg-white/5 dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg backdrop-blur-sm border border-white/10">
            <h3 class="text-2xl font-semibold mb-2 text-purple-400">3. Extract the Add-ons</h3>
            <p class="text-lg leading-relaxed text-white">
                Locate the downloaded <code>.zip</code> file in your file explorer, right-click on it and select
                <em>Extract Here</em> (or <em>Extract All</em>). This will create a folder containing all the add-ons individually.
            </p>

            <div class="grid md:grid-cols-3 gap-6 items-center">
                <figure class="flex flex-col items-center gap-2">
                    <img src="/img/tuto/zip-in-explorer.png" alt="Zip file in file explorer" class="rounded-lg shadow-md object-cover">
                    <figcaption class="text-sm text-grey-400 italic">Locate the downloaded archive.</figcaption>
                </figure>

                <figure class="flex flex-col items-center gap-2">
                    <img src="/img/tuto/extract-option.png" alt="Extract option right-click" class="rounded-lg shadow-md object-cover">
                    <figcaption class="text-sm text-grey-400 italic">Right-click and choose “Extract Here”.</figcaption>
                </figure>

                <figure class="flex flex-col items-center gap-2">
                    <img src="/img/tuto/extracted-folder.png" alt="Extracted add-ons" class="rounded-lg shadow-md object-cover">
                    <figcaption class="text-sm text-grey-400 italic">Result: each add-on extracted as a file.</figcaption>
                </figure>
            </div>
        </article>

        {# Étape 4 — Installation dans Blender #}
        <article class="bg-white/5 dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg backdrop-blur-sm border border-white/10">
            <h3 class="text-2xl font-semibold mb-2 text-purple-400">4. Install Add-ons in Blender</h3>
            <p class="text-lg leading-relaxed text-white">
                Open Blender, go to <strong>Edit → Preferences → Add-ons</strong> and click <em>Install from Disk</em>.
                Select all the extracted add-ons, confirm, and Blender will install them automatically.
            </p>

            <div class="grid md:grid-cols-2 gap-6 items-center">
                <figure class="flex flex-col items-center gap-2">
                    <img src="/img/tuto/blender-main.png" alt="Blender main interface" class="rounded-lg shadow-md object-cover">
                    <figcaption class="text-sm text-grey-400 italic">Blender main interface.</figcaption>
                </figure>

                <figure class="flex flex-col items-center gap-2">
                    <img src="/img/tuto/blender-install.png" alt="Add-ons installation window" class="rounded-lg shadow-md object-cover">
                    <figcaption class="text-sm text-grey-400 italic">Add-ons installation window.</figcaption>
                </figure>
                <figure class="flex flex-col items-center gap-2">
                    <img src="/img/tuto/selected-file.png" alt="Add-ons selected" class="rounded-lg shadow-md object-cover">
                    <figcaption class="text-sm text-grey-400 italic">Add-ons installation window.</figcaption>
                </figure>
            </div>

            <cite class="mt-4 border-l-4 border-purple-500 pl-4 italic text-grey-400">
                Tip: you can select multiple add-ons at once by holding <kbd>Ctrl</kbd> (Windows) or <kbd>Cmd</kbd> (macOS).
            </cite>
        </article>
    </section>

    {# Comment crée une collection #}
    <section class="p-8 mt-8 flex flex-col gap-12 w-full xl:w-2/3 mb-16 bg-grey-800 rounded-lg">
        <h2 class="text-4xl font-semibold text-white">How to Create Your Own Collection</h2>

        {# Étape 1 — Créer un compte #}
        <article class="bg-white/5 dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg backdrop-blur-sm border border-white/10">
            <h3 class="text-2xl font-semibold mb-2 text-purple-400">1. Create an Account</h3>
            <p class="text-lg leading-relaxed text-white">
                To start creating your own collections, you first need to <a href="{{ path('app_register') }}" class="text-purple-400 underline hover:text-purple-300 transition">create an account</a>.
                It allows you to save, edit, and share your collections with the community.
            </p>

            <figure class="flex flex-col items-center gap-2">
                <img src="/img/tuto/create-account.png" alt="Account creation form" class="rounded-lg shadow-md w-full md:w-4/5 object-cover">
                <figcaption class="text-sm text-grey-400 italic">Account creation form on Blender Collection.</figcaption>
            </figure>
        </article>

        {# Étape 2 — Accéder à la création de collection #}
        <article class="bg-white/5 dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg backdrop-blur-sm border border-white/10">
            <h3 class="text-2xl font-semibold mb-2 text-purple-400">2. Access the “Create Collection” Page</h3>
            <p class="text-lg leading-relaxed text-white">
                Once logged in, click on the <strong>Create Collection</strong> button in the navigation bar to open the creation form.
            </p>

            <figure class="flex flex-col items-center gap-2">
                <img src="/img/tuto/nav-create-btn.png" alt="Navigation bar create button" class="rounded-lg shadow-md w-full md:w-4/5 object-cover">
                <figcaption class="text-sm text-grey-400 italic">The “Create Collection” button in the navigation bar.</figcaption>
            </figure>
        </article>

        {# Étape 3 — Ajouter les add-ons #}
        <article class="bg-white/5 dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg backdrop-blur-sm border border-white/10">
            <h3 class="text-2xl font-semibold mb-2 text-purple-400">3. Add Add-ons to Your Collection</h3>
            <p class="text-lg leading-relaxed text-white">
                In the creation form, add your favorite add-ons one by one by pasting their links from 
                <a href="https://extensions.blender.org" target="_blank" class="text-purple-400 underline hover:text-purple-300 transition">Blender Extensions</a>.
                You can reorder or remove them at any time before saving.
            </p>

            <figure class="flex flex-col items-center gap-2">
                <img src="/img/tuto/create-form.png" alt="Collection creation form" class="rounded-lg shadow-md w-full md:w-4/5 object-cover">
                <figcaption class="text-sm text-grey-400 italic">Form to add add-ons and name your collection.</figcaption>
            </figure>
        </article>

        {# Étape 4 — Éditer la collection #}
        <article class="bg-white/5 dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg backdrop-blur-sm border border-white/10">
            <h3 class="text-2xl font-semibold mb-2 text-purple-400">4. Edit Your Collection</h3>
            <p class="text-lg leading-relaxed text-white">
                After saving, you can view your collection’s page. From there, you can edit its name, description, or add/remove extensions directly.
                Changes are saved instantly.
            </p>

            <figure class="flex flex-col items-center gap-2">
                <img src="/img/tuto/collection-edit.png" alt="Editing the collection page" class="rounded-lg shadow-md w-full md:w-4/5 object-cover">
                <figcaption class="text-sm text-grey-400 italic">Editing your collection directly from its detail page.</figcaption>
            </figure>
        </article>
    </section>

    {# Comment contribuer #}
    <section class="p-8 mt-8 flex flex-col gap-12 w-full xl:w-2/3 mb-16 bg-grey-800 rounded-lg">
        <h2 class="text-4xl font-semibold text-white">Contributing to Blender Collection</h2>

        {# Étape 1 — Introduction à la contribution #}
        <article class="bg-white/5 dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg backdrop-blur-sm border border-white/10">
            <h3 class="text-2xl font-semibold mb-2 text-purple-400">1. Why Contribute?</h3>
            <p class="text-lg leading-relaxed text-white">
                Blender Collection is an open-source project built by and for the Blender community.  
                Whether you're a developer, designer, or technical artist, your help can improve the platform,
                expand the features, or enhance the user experience.
            </p>

            <p class="text-lg leading-relaxed text-white">
                Contributions can include <strong>bug fixes</strong>, <strong>new features</strong>, <strong>documentation improvements</strong>,
                or even <strong>translations</strong>. Every pull request counts.
            </p>

            <figure class="flex flex-col items-center gap-2">
                <img src="/img/tuto/github-banner.png" alt="GitHub repository banner" class="rounded-lg shadow-md w-full md:w-4/5 object-cover">
                <figcaption class="text-sm text-grey-400 italic">Join the project on GitHub and help make Blender Collection better.</figcaption>
            </figure>
        </article>

        {# Étape 2 — Cloner et installer le projet #}
        <article class="bg-white/5 dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg backdrop-blur-sm border border-white/10">
            <h3 class="text-2xl font-semibold mb-2 text-purple-400">2. Getting Started</h3>
            <p class="text-lg leading-relaxed text-white">
                The project is hosted on GitHub at:
                <a href="https://github.com/LINDECKER-Charles/BlenderAdd-OnListe" target="_blank" class="text-purple-400 underline hover:text-purple-300 transition">
                    github.com/CharlesLindecker/Blender-Collection
                </a>
            </p>

            <p class="text-lg leading-relaxed text-white">
                To get started, clone the repository and install the dependencies:
            </p>

            <pre class="bg-black-950 text-white p-4 rounded-lg text-sm overflow-x-auto"><code>
    git clone https://github.com/LINDECKER-Charles/BlenderAdd-OnListe.git
    cd app_blenderCollection
    git checkout dev
    composer install
            </code></pre>

            <p class="text-lg leading-relaxed text-white">
                You can then run the local development server with:
            </p>

            <pre class="bg-black-950 text-white p-4 rounded-lg text-sm overflow-x-auto"><code>
    docker compose up
    docker compose exec app composer install
    docker compose exec app php bin/console doctrine:migrations:migrate
    docker compose exec app php bin/console messenger:consume async -vv
            </code></pre>
        </article>

        {# Étape 3 — Proposer une contribution #}
        <article class="bg-white/5 dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg backdrop-blur-sm border border-white/10">
            <h3 class="text-2xl font-semibold mb-2 text-purple-400">3. Making a Contribution</h3>
            <p class="text-lg leading-relaxed text-white">
                Once your environment is ready, create a new branch describing your modification:
            </p>

            <pre class="bg-black-950 text-white p-4 rounded-lg text-sm overflow-x-auto"><code>
    git checkout -b feature/add-new-filter
            </code></pre>

            <p class="text-lg leading-relaxed text-white">
                When your changes are complete, push your branch and open a Pull Request on GitHub.
                Please describe what your PR does and link it to an issue if possible.
            </p>

            <p class="text-lg leading-relaxed text-white">
                Don’t hesitate to discuss your ideas in the 
                <a href="https://github.com/LINDECKER-Charles/BlenderAdd-OnListe/issues" target="_blank" class="text-purple-400 underline hover:text-purple-300 transition">
                    Issues section
                </a>.
            </p>
        </article>

        {# Étape 4 — Bonnes pratiques #}
        <article class="bg-white/5 dark:bg-black-900 p-8 rounded-lg flex flex-col gap-6 shadow-lg backdrop-blur-sm border border-white/10">
            <h3 class="text-2xl font-semibold mb-2 text-purple-400">4. Guidelines & Best Practices</h3>
            <ul class="list-disc pl-8 text-white text-lg space-y-2">
                <li>Follow PSR-12 coding standards for PHP and use clean, readable commits.</li>
                <li>For front-end changes, respect the existing Tailwind theme and design tokens.</li>
                <li>Keep commits small and atomic — one change per commit.</li>
                <li>If you add or change features, update the documentation accordingly.</li>
            </ul>

            <p class="text-lg leading-relaxed text-white">
                Every contribution helps the project grow. Thank you for your time, your ideas, and your energy 💜
            </p>
        </article>
    </section>

{% endblock %}